/**
 * Dynamic Types - JS is a weakly typed language
 */

/**
 * Let's verify the type of clima variable (string)
 */

let clima = "Quente";

console.log(typeof clima);

/**
 * If I change the value of clima variable, the type will change too (number)
 */

clima = 15;

console.log(typeof clima);